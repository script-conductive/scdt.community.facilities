import React from "react";

const Image = (props) => {
  return (
    <div>
        { props.record.params.background_image ? <img src={props.record.params.background_image} alt="image" style={{width: '50px'}} /> : null }
    </div>
  );
};

export default Image;
